services:
  nexa-bot:
    container_name: Nexa
    build: .
    restart: unless-stopped
    env_file:
      - /home/envs/nexa.env
    volumes:
      - ./logs:/app/logs
    networks:
      - nexa-network

networks:
  nexa-network:
    driver: bridge
